package seedu.address.testutil;

import seedu.address.model.ModelManager;

/**
 * A mock version of the ModelManager used for testing.
 * It allows the testing classes to access a mock ModelManager for testing
 * purposes,
 * without interacting with the actual ModelManager layer.
 */
public class MockModelManager extends ModelManager {
}
